[package]
name = "game"
version = "0.1.0"
edition = "2021"

[dependencies]
agones = { version = "1.34", optional = true }
bevy = "0.14"
bevy_egui = { version = "0.30", optional = true }
bevy-inspector-egui = { version = "0.27", optional = true }
bevy_mod_picking = { version = "0.20", optional = true }
bevy_mod_reqwest = "0.16"
bevy_rapier3d = { version = "0.27", default-features = false, features = [
    "dim3",
    "simd-stable",
] }
naia-bevy-client = { version = "0.23", features = [
    "transport_udp",
], optional = true }
naia-bevy-server = { version = "0.23", features = [
    "transport_udp",
], optional = true }
naia-bevy-shared = "0.23"
tokio = { version = "1.40", features = ["full"], optional = true }

common = { path = "../common" }

[features]
default = [
    "client",
    #"server",
]
client = [
    "dep:bevy_egui",
    "dep:bevy-inspector-egui",
    "dep:bevy_mod_picking",
    "bevy_rapier3d/async-collider",
    "bevy_rapier3d/debug-render-3d",
    #"dep:naia-bevy-client"
]
server = [
    "bevy_rapier3d/headless",
    #"dep:naia-bevy-server",
    "dep:agones",
    "dep:tokio",
]

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

# Enable more optimization in the release profile at the cost of compile time.
[profile.release]
# Compile the entire crate as one unit.
# Slows compile times, marginal improvements.
codegen-units = 1
# Do a second optimization pass over the entire program, including dependencies.
# Slows compile times, marginal improvements.
lto = "thin"

# Optimize for size in the wasm-release profile to reduce load times and bandwidth usage on web.
[profile.wasm-release]
# Default to release profile values.
inherits = "release"
# Optimize with size in mind (also try "z", sometimes it is better).
# Slightly slows compile times, great improvements to file size and runtime performance.
opt-level = "s"
# Strip all debugging information from the binary to slightly reduce file size.
strip = "debuginfo"
